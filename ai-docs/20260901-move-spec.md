# move spec（已拍板）＋ planarize 深模块提取案

> as-of v0.0.5 / 2026-09-01（创建日）· created by Claude Fable 5
> 来源：2026-09-01 user 口述 + 逐轮 grill 拍板（本文全部裁决有当日对话出处）。
> 取代 `20260901-move-postmortem.md` §4 的未拍板提案。旧 moveVertices 五病见该文 §3。

## 1. 管线四步（全部批量、只看终态）

1. **刚性平移**：选中实体集（顶点/边/面**混选合法**，SU 同款）→ 顶点并集 → 一个 δ 纯改坐标，
   共角边橡皮筋跟随。零裁决。
2. **planarize（自愈）**：见 §2。
3. **face-finding**：受影响平面重跑区域提取。
4. **设置面（覆盖指示函数）**：胞腔填 ⟺ 被 ≥1 条旧膜环循环覆盖。

拖拽中渲染 = **影子副本 WYSIWYG**（clone+同套 mutation+diff——「预览即提交」；user 2026-09-02
黄线「SU 无门槛理念 WYSIWYG」，取代早前「纯 ghost」条款）；拓扑结算仍只在松手跑 1-4。

## 2. planarize 契约（对抗式表述，user 2026-09-01）

「对抗攻击者瞎动你的顶点和膜注册表——但不许破坏更底层的引用完整性——然后你自愈。」

- **前置（攻击面）**：顶点坐标任意脏（交叉/骑点/重合/共线重叠/退化）；膜注册表 stale。
- **保底（不许碰）**：拓扑层引用完整性（边表/顶点表/膜环引用不悬空）。
- **后置**：平面性不变量全恢复（无边边交叉、无点骑边、无重合顶点、无共线重叠、无平行重边）；
  膜环经切割血缘重写为合法闭循环；事件叙事流照发。
- **确定性**：输出只依赖终态几何（arrangement 规范性），与调用顺序/历史无关。

**深模块提取**：planarize 是两动词共享的裁决机——
画笔 = 加线段 + planarize（**注入手势边集**→BIRTH 依据）；move = 改坐标 + planarize（注入空集→永不生膜）。
insertSegment 降格为单线段增量特例。血缘管道（父边→子边）同时喂膜环重写与手势身份传承。
**协议泛化（user 2026-09-01 拍板）**：rotate / duplicate / scale 未来全走此协议——「就是 sticky
geometry 嘛」。变换类动词 = 改坐标 + 无意图自愈；duplicate = 注入拷贝几何与拷贝膜注册项 + 无手势自愈。
**测试红利**：契约支持 fuzz 攻击测试（随机扰动→planarize→断言全不变量），golden 靶之外的第二道网。

## 3. 覆盖代数（user 逐条拍板）

- **1+1=1**（OR；不 mod 2、不看方向、不看次数）。「膜没有出身，填着就是填着。」
- 实机证词（user 2026-09-01）：2D 小矩形移进大矩形不挖洞；**3D 双立方拼合，共享面被分割但不挖洞**
  （截图证词，与此前 stuck-cube dedup 单膜结论互证）。
- 压扁膜 = 环覆盖空集 → 自然无膜（指示函数推论，非特例规则）；日志发 BURST 纯属叙事。
- 自交环（蝴蝶结）：planarize 先自切，交点成顶点；两瓣 |绕数|≥1 → 全填。伪问题已消解。
- id/事件 = 叙事层：一对一保 id（STRETCH）、一对多 DIVIDE、多对一 MERGE（dedup）带血缘。

## 4. backlog（user 指示入册，暂不设计）

- **push-pull 挖洞特例**：覆盖代数唯一的例外来源——SU 里洞只由 push-pull 打穿产生
  （移动/拼合永远 1+1=1）。归 M4 push-pull 轮与暗礁②一起思考。实现时挂 golden todo 占位。

## 5. golden 靶清单（实现前先红）

1. 验尸重现①：整移边跨静态边 → 直达终位、S 原样、无幽灵切点、无吞 move。
2. 验尸重现②：边移进膜内 → 膜边界原样、膜活、悬边落内。
3. 蝴蝶结：单顶点拖穿对边 → 自切成两瓣、两瓣全填（DIVIDE 叙事）。
4. 压扁：膜移成零面积 → 无膜 + BURST 叙事。
5. 重叠 dedup：填膜移压填膜 → A/O/B 三膜全填、O 单膜（MERGE 叙事）；与「静态叠画两矩形」终态逐胞腔等价。
6. 守恒律回归：move 闭合开折线不出面（既有 golden 保绿）。
7. 多选混移：线+面混选一批刚移，等价于顶点并集单 δ。
8. fuzz：随机扰动 N 轮 → planarize → 不变量全绿。

## 6. 立方体移墙案（2026-09-01 同日修，edited by Claude Fable 5）

user 实测报「移动侧面之后顶面没跟上」。验尸：快路径（无合并无切割时）只 refreshRingPts，
**不检测出平面/平面平移**——非平面膜被投影假装还平（顶面渲染留在旧平面）、法向平移的膜
planeId 陈旧。修法：**动到任何膜的顶点必走覆盖 reconcile 全路径**；全路径里
①膜像仍平但换了平面 → 重拟合跟随（退休铸新，计入 STRETCH 叙事）
②膜像真非平面 → 拟合失败零认领 → BURST 曝光（autofold 空档的诚实语义，不静默）。

意外收获：移整面产生的「非平面」大多是**假非平面**——移墙剪切后顶/底面倾斜但仍是平面
（z 线性于 x），重拟合直接跟随，六膜全活。

**autofold 已落地（2026-09-01 同日，user 裁「现有操作集必须有，和 pp 无关」）**：
规则=**最少折缝**（user 拍板：「十边形对折应该只出一条缝」，不切三角雨）——沿环找极大共面段，
段边界结点连折缝弦，每片自成平面膜；k≥3 时结点多边形自成一片（非平面则分解失败）。
位置=sticky 协议自愈步第二员（`src/kernel/autofold.ts` + moveVertices ③.5；planarize 治交叉、
autofold 治非平面）；认领按片（各片自己的平面）、叙事=DIVIDE（膜折而不破——「move 绝不破膜」
至此完整成立）；带洞面/分解失败 → BURST 兜底诚实退路。golden：顶角折两片一条缝、十边形对折一条缝。
