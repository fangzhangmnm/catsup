# 三阶段路线：收口 → 求解器数学 → push/pull grill

> as-of v0.0.13 / 2026-09-01（创建日）· created by Claude Fable 5
> user 定调：「收好口，然后把求解器的数学弄干净变成很好定义很容易说清楚的形式，
> 然后开始 grill push pull」。本文=三阶段的展开细目（当日对话详版）。
> 战略链条：**收口给求解器腾手术台；求解器给 pp 腾地基**（pp 的新约束类型必须落在干净求解器里）。

## 阶段一：收口（封箱 0.0.x）

- ~~ε 分层+磁滞~~ 已裁落地（v0.1.0）。
- ~~封 0.1.0~~ 已批已封。
- ~~暗礁①~~ 已结案（user 确认）；~~空落点~~ 已裁=学矩形摄像机托底。
- ~~autofold~~ 已裁已落（v0.1.1）：最少折缝规则（user 拍板十边形一条缝），详 move-spec §6；
  暗礁① ABSORB/BURST 语义=贴图/属性跟随生死簿（user 确认）。收口阶段完结 ✅ → 下一站阶段二求解器。
- 【并入阶段二】倾斜平面面内共轴（方向集显式化后=一行注册：世界 XYZ ∪ 画面基 u/v）

## 阶段二：求解器数学 —— **核心已落地 v0.1.2（2026-09-01）**

落地：`src/playground/solver.ts`（solvePoint 三步字典序 + buildConstraints + 面内轴注册）；
snapPoint=兼容壳；property 测试 5 件全绿（解在 ε 邻域 200 点扫描/确定性/解释完整/字典序/秩表哨兵）。
尾巴（不挡 pp grill）：resolvePlane 并入同机、调用方直迁后删壳。

### 目标形态（原案，已实现）

**纯函数 `solve(V, s, C) → (p, 解释, 提示)`**：
- C = 约束集；约束 = {轨迹 L⊆ℝ³（0/1/2-D）, 语义秩 rank, 捕捉半径 ε}。
- 三步求解：①活跃过滤（屏距≤ε(c)）②降维合成（活跃 1-D 两两真相交→涌现 0-D；不级联）
  ③字典序选择（维度升，rank 降，屏距升）。
- 不变量（property 测试）：解点在光标 ε 邻域内；同输入同解；解释完整（tooltip/提示线自动生成，无第二套真相）；
  1-D 来源有限且用户指定（幽灵 align 定理→前置条件）。
- 物理落点：solver.ts 纯函数 + 情境构建器（工具声明需求=单一供餐律物理化）+ 外层状态壳（充能/磁滞）；
  snapPoint 降级兼容壳后删除；rect 平面三规则=同机器的 resolvePlane 查询。
- 作为 pp 前置：pp 需要「面法向轴」「到另一面高度」两个新约束类型（drill 点名过），注册进 C 即可。

## 阶段三：push/pull —— **v1 已落地（2026-09-01，kernel.pushPull 宏动词）**

灵魂手势 golden 达成（矩形+pp 逐字等价手绘立方体）；挖洞/暗礁② 待真机
（执行档=`20260901-pushpull-grill-sheet.md` §2 实验清单 E1-E6）。

### 原议程（1/4/5 已落，2/3 待实机）

1. 动词语义核：pp 能否骑 sticky geometry 协议——意图注入参数给「侧壁+顶面身份」（协议第三个客户+压力测试）。
2. 挖洞特例：覆盖代数 1+1=1 的唯一合法例外，来源=意图不是几何（pp=家族唯一减法动词/负膜注入）——框架成立与否是核心裁决。
3. 暗礁②：非流形撞合膜归属（BURST+BIRTH vs DEDUP-MERGE）+ 真机测试清单。
4. inference 配套：法向轴锁/对面高度 snap/数值输入（VCB）的 v1 范围。
5. 验收灵魂手势：矩形+pp 三手势出立方体；golden 钉「与手绘六面逐字等价」。
