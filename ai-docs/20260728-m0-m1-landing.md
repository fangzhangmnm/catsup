# M0-M3 落地记录 —— 内核 + golden corpus + playground + 工具动词 + 升维

> as-of 0.0.4 / 2026-07-28（创建日）
> 读者：下一个在 CatsUp 干活的 session。前情见 `20260728-phase0-kickoff-handoff.md`；spec 本体 = `journals/20260627 SketchUp drill.md`（只读）。

## 交付了什么

- **M0**：git repo 诞生、工具链抄 WebPaint 现役（`test/runner.mjs` 零依赖 runner、`scripts/build.sh` esbuild auto-curl + tsc 门 + experiments 隔离 lint、tsconfig 全树 glob）。
- **M1**：`src/kernel/` 六模块 + golden corpus + `src/playground/`（事件日志与 golden 同源）。
- **M2 工具动词**：选择（点选/框选 window 语义 + Delete 批量删、Esc 取消）/ 画线 / 矩形 / 移动（顶点/边/整面平移，一批 moveVertices）/ 橡皮（刮擦收集一批擦——批量裁决语义现场可见）/ 删面。**inference 平面版四标本** = `src/playground/inference.ts`：endpoint > midpoint > on-edge > axis（有 anchor 才轴锁；SU 配色 endpoint 绿/midpoint 青/on-edge 红/X 红 Y 绿虚线）。结构上是**所有取点动作共用的输入前置层**（验收③的门），不进 kernel——engine 系统化是下一个纪元；勘探发现人类自己记 journals。
- 纯逻辑（rectSegments/marqueePick/moveTargets/translateMoves/scrubHits/inferPoint）在 DOM-free 模块，node 直测；main.ts 只做指针接线。测试 55 绿 5 todo。
- 验收①②达成（工具层闭环）：Rect 工具 = `rectSegments` → `addEdges(4 段)`，全链路无任何 Face 构造；dividing/MERGE/ABSORB/BURST 全部落手即生效；dissolve-collinear 无自动路径（靠缺席实现不对称律）。

## M3 升维（2026-07-28 当天二连）

- **内核 3D 化**：拓扑存 Pt3；`planes.ts` = coplanarity 分组（cover 非 partition——一边可入多组=非流形来源）+ **PlaneRegistry 平面 sticky 身份**（τ 容差合并 + canonical 平面 → **确定性正交基** → 投影坐标跨重跑稳定，2D face-finding 管线经 `findRegions(scope)` 投影原样复用）。
- **τ = coplanarity 容差旋钮**（Kernel opts，默认 1e-3）：只管平面分组/归属；几何保持精确共面——把点拍到平面上是输入侧（pick/snap）的职责（drill L297 结论，防 τ 松了 PSLG 被斜边打脏）。
- **radial erase 裁决**：命中面先按平面分组再裁决——同平面两面共享→MERGE；不同平面各持一面→各自外环对 void→**双 BURST**（立方体擦棱行为）。cube golden：底+四墙手绘，第四面墙封口时**顶面自动涌现**（闭合空环即生膜——SU 真机同款）。
- **渲染选型已拍板（用户 2026-07-28）**：three.js vendored（0.185.1，`src/vendor/three/`），**不抄 RealHome**；3D 库模块解耦慢慢攒。边界纪律：three 只在 `render3.ts` 出现；`camera.ts`（纯数学正交轨道）与 `pick.ts`（屏幕空间拾取+吸附，射线遮挡序）零 three 依赖、node 直测。遮挡交 z-buffer（用户：canvas painter 排序才是大坑）。
- **playground 3D**：中/右键环绕、Shift 平移、滚轮缩放；画线平面=手势按下时锁定（面下画在面平面，否则地面）；轴锁开放全部世界轴——**Z 轴锁是画第一条竖直边的唯一入口**（SU 蓝轴）；preview 影子副本机制原样带过来。
- 明确没做：push/pull（M4）、move 只在抓取平面内、拖出平面=autofold 空档（todo 占位）、倾斜平面回归（todo）。

## 模块地图（上层只 import 下层）

```
src/kernel/
  kernel.ts          批量 facade（PtIn z 缺省 0；τ 旋钮；clone 影子副本；faceRings3 给渲染）
  face-lifecycle.ts  心脏：存储态 face reconciler（7 事件，M3 起 per-plane + radial 裁决）
  planes.ts          coplanarity 分组 + PlaneRegistry（平面 sticky 身份 + 确定性基）
  facefind.ts        纯区域提取（scope: 边子集+投影；wedge/最紧转弯/嵌套挂洞）
  subdivide.ts       sticky 插入 + 3D 打交点切边（B–O 换入点）
  topology.ts        PlanarGraph 3D（faceLinks 变长 = radial cycle 本体）
  geom.ts            2D+3D 数值层（量化/平面/基/求交）
src/playground/
  camera.ts          纯数学正交轨道相机（零 three）
  pick.ts            屏幕空间拾取+吸附（零 three；射线遮挡序；axis 锁含 Z）
  render3.ts         three 适配层（three 只在此出现；z-buffer 遮挡）
  tools.ts           工具纯逻辑（rectSegmentsOnPlane / moveTargets / translateMoves）
  main.ts            指针接线
```

## preview 架构（影子副本预演，2026-07-28 用户要求「引擎支持 preview」后定）

**preview = `kernel.clone()` → 在副本上跑同一套 mutation → 渲染 diff → 松手对真身重放。**
- 为什么不写第二套"预测"路径：公理 5（急切无历史）→ 内核永远整定态、没有半应用状态；预测代码与真引擎必然漂移 → 预览撒谎（煤气灯红线）。影子副本天然「预览即提交」。
- id 计数器随 clone 一起拷 → **副本预演的事件与真身提交的事件逐字相同**（`test/preview.test.ts` 钉死，含不泄漏测试）。
- 成本：每次 pointermove 全量 clone——M1 规模无压力；将来大模型的增量化与「局部 face-finding」是同一条命门，同一接缝解决。
- playground 侧：hover 即显示 inference 指示+中文 tooltip（端点/中点/边上/水平/垂直）；拖拽中渲染 diff 覆盖层（会死的面灰罩、新生/变形面蓝虚线+淡填充、新增/移位边蓝虚线 ghost），hint 栏实时显示「预览：分割 面#3 → #4 + #5」这类事件预告；删面 hover 即预览灰罩。

## 关键设计决策（为什么这么做）

1. **face 是存储态非派生量**（公理 4 修正版）→ findRegions 输出不直接替换 face 集，而是按 mutation 类型 reconcile。face↔region 匹配用**几何 anchor**：面的 representativePoint（扫描线取内点）落在哪个新 region 就是后继。region 互不重叠 → 无歧义。
2. **手势边语义**是 BIRTH 的裁决依据：`created`（沿画线新生）+ `retraced`（与既有边重合）是手势边；**既有边被切开的子边不是**（T 触碰空环不生膜）。手势边被同批后续段切开时经 onSplit 回调继承身份。BIRTH 只看无主 region 的**外环**是否含手势边（在空环里画小方生的是小方，不是外圈环带）。
3. **erase 无条件按 loop 身份裁决，不做覆盖查找**：外环对 void 被擦 = 无条件 BURST。若做覆盖查找，回字内填外空擦内边会把面错误长满外方（「intention 被结构化成了 loop membership」）。内环/桥缝 = 存活整理（ABSORB）；两面共享 = MERGE（union-find 支持批量链式并）。
4. **批量语义**：裁决快照以批开始时的环结构为准，再统一删除（删除顺序无关）。并组被 burst 污染 → 整组死——与逐条串行擦的任一顺序结果一致（测试钉了）。
5. **move 守恒零成本**：moveVertices 不传手势边集 → 结构上不存在 birth 路径。sticky 三件套：落点撞顶点=合并、落他边内部=切开再并、拖出交叉=rip & reinsert。
6. **id 纪律**：DIVIDE/MERGE/ABSORB 退休旧 id 铸新 id（事件携带血缘）；延续/STRETCH 保 id。
7. **局部性接缝已留**：`findRegions(g, region?)` 的 region 参数 M1 忽略（整平面重跑）；增量局部 face-finding（spec 点名的工程命门）以后在接缝内换入，接口不变。

## 暗礁①已钉推导值（待真机 SU 裁决）

三层回字（填-空-填）：**擦中环边 = ABSORB**（外膜长满、内方岛经重嵌套变成它的洞）；**擦内环边 = BURST**（内方邻接空环带=void，破防；外环带不受牵连）。两个方向都有 golden 测试。若真机证伪：只改 `face-lifecycle.ts` 裁决规则 + `test/golden.test.ts` 对应断言。

## 明确没做的（都有 todo 测试占位）

- move 拖边横穿他面的急切 dividing；move 把面压扁成零面积 → 静默移除（超范围）。
- DEDUP-MERGE / stuck cube / autofold（3D，M4）；暗礁②（push/pull 非流形撞合）。
- Bentley–Ottmann；增量局部重算（接缝都留好）。
- push/pull（M4 的主角）；move 拖出抓取平面（autofold 空档）。

## 跑法

```
npm test        # 68 绿 6 todo
npm run build   # tsc 门 + experiments lint + playground bundle
# 浏览器直接开 src/playground/index.html（file:// 即可，无 SW）
```

## 收工状态（2026-07-28 当晚，用户裁定）

**这版还不可用。** 用户评价：软件工程层面 competent，问题全出在「需要 grill、写 spec」的层面——
乱七八糟的行为太多，得**系统地 grill**，不是继续堆码。反馈原文 = `journals/20260728 phase0 feedback thread.md`（只读）：
- snap 要加「和点共轴」（SU 的 from-point 推断）；整个 snap 体系（种类/优先级/捕捉关系）需要系统 grill；
- 多自由度时要能**多约束组合**（例：画一条垂线落到边上 = 垂直约束 + on-edge 同时成立）——当前单一 SnapKind 的形状装不下这个；
- move 时边撞到面的行为很奇怪（sticky move 的 rip&reinsert 路径没有 spec 依据，是我的发明——这类没有实机依据的行为就是「乱」的来源）。

**下一个 session 的正确开工姿势**：不是写码，是拉着用户 grill（有 grill-me skill）——很可能先**回 2D** 把 snap 体系、
约束组合、move 撞击行为逐个钉成 spec/golden，再回 3D。当前 inference 四标本只是占位标本，别在它上面继续长。

2D 时代：画矩形即填 → 对角线即分两面 → 擦日字共边并一面 → 方中画方成环带+内方 → 删内方膜再擦洞边 ABSORB 愈合 → 事件日志逐条中文、全程无确认步。
3D 时代（M3）：中/右键环绕 + 滚轮缩放顺不顺手 → 地面画矩形即填 → 画线从角点起、往上拖出「Z 轴」蓝提示画竖线 → 两竖一横封口墙面自动涌现 → 在墙面上画矩形（画线平面=墙）→ 擦地墙共享棱两面齐破 → hover 吸附提示（端点/中点/边上/三轴）全程先于按键出现。
