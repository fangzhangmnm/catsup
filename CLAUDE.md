# CatsUp（家族总规则见上级 CLAUDE.md）

SketchUp 开源 PWA 平替。**MVP = SketchUp clone；proposal「我会加的东西」整节全是花，不可本末倒置**（`journals/20260627 CatsUp proposal.md` L80 用户原话）。

- **当前纪元：0.4 VR 纪元（2026-09-07 开工，user 拍板「0.4.0 优先做 vr…逼出后面的每一个设计都 vr 第一公民」；挂机轮已落 v0.4.0，edited by Claude Fable 5.1）**：`src/player/` = 玩家深模块（固定 60 Hz 状态机、胶囊+悬挂、抛射体 teleport、freeze/coyote 冻结机制、键鼠/手柄两个输入适配器；零 kernel/three），`src/editor/collision-world.ts` = 膜 → WorldQuery，`src/editor/xr-pointer-frame.ts` = 控制器射线当指针（PointerFrame 第二实例，求解器零改动），`src/app/{locomotion,vr}.ts` + `ui/{hud-model,wrist-panel}.ts` = 接线与手腕面板（桌面 HUD 与面板同一份数据模型）。**桌面「步行相机」= 不戴头显验证移动/碰撞/teleport 的通道**；VR 逻辑由假会话探针 `test/probes/probe-xr-fake.mjs` 钉死，真机只验会话进出。指路牌 = `ai-docs/20260907-vr-epoch-handoff.md`（§8 回填 / §9 真机首轮）；待 user 的 12 条在总账 A13 末尾。**v0.4.1（09-07 晚，真机首轮反馈 A16）**：内部单位米落到眼睛上（网格 1 m、默认相机半高 4 m、zoom 下限 1 mm）；`render3.ts` = **retained mode**（常态 3 个 draw call、几何按 revision/预演身份/选区键缓存、零每帧分配——别退回每帧重建）；**VR 拾取的视口平面模型已判不鲁棒，动 `xr-pointer-frame.ts`/solver 度量前先读 `ai-docs/20260907-vr-input-reflection.md`（待拍板；输入只有手的射线，可见性也从手判，不看头）**。v0.4.2：内核 sticky 插入容差对齐（`INSERT_TOL`，立宪页 §5）+ 错误边界三层（编辑器/循环/全局，不吞、VR 字幕位 toast）+ 1/z 止血 + `frame().ray/angularPx` 必配 `fvp()` 纪律（**build.sh 0.65 flatscreen 护栏 lint 机械执法**）。v0.4.3：`test/kernel-fuzz.test.ts` 随机操作 fuzz = 内核拓扑不变量的守卫（改 face-lifecycle 必跑；残余重复面 5 种子 todo = 总账 D-fuzz）。
- **前纪元：0.3 app 壳纪元（2026-09-06 开工，edited by Claude Fable 5.1）**——lab 退役，正式 app = 根 `index.html` + `src/app/`，**UI = HUD**（user 拍板：顶栏/状态栏是浮在满屏视口里的胶囊，像游戏 HUD，为将来 VR 无痛）；**无地骑士：零持久化**（无 store / 无自家文件格式，user 2026-09-06「现在定文件格式还太早」），模型只活在内存，**OBJ 是唯一逃生口**（`src/editor/obj-io.ts`，Blender 往返；不是文件格式）。**iPad 一等公民**（pen=工具、手指=相机、双指轻点撤销；`src/app/gestures.ts`）。**预期快速重构**（user 原话：lab 期间忍着没提的 UX 需求现在会陆续来）。
- **上线道 = 公开工坊道（user 2026-09-06：「上线就是和 weebpaint 一样，源码和 ai docs 应该也有，就是别人可以接手的状态」；隐私分拣已跑：journals 从未入史、无密钥/绝对路径）**：源仓公开于 GitHub `fangzhangmnm/catsup`（远端 `github`；`origin` = OneDrive bare 备份，**两边都推**）。部署 = WeebPaint 同款 `.github/workflows/deploy.yml`（main→`/dev/`，prod→`/`；prod 分支未建时根为占位页；不重 build，各分支自己 commit 好 dist）。日常：`bash scripts/build.sh` → commit → `git push github main && git push origin main` → 约 1 分钟后 **https://fangzhangmnm.github.io/catsup/dev/**。push prod = `git push github main:prod`，硬规则 #5 必问；有 prod 后再抄 WeebPaint `kick-pages.sh`（两 ref 同 sha 的 Pages 去重坑）。**Pages 部署出问题先读家族 SSoT `../20260524 WeebPaint/ai-docs/20260530-lessons-pages-deploy-race.md`（坑一 race / 坑二 同 sha 去重 / 坑三 `Multiple artifacts named "github-pages"` 硬失败——2026-09-07 ac0cacc 本仓撞过，处方 = `gh workflow run "Deploy Pages" --ref main` 起新 run，别 rerun 同 run）；验证一律探 content-hash bundle URL（`/dev/dist/catsup-<hash>.mjs?probe=$RANDOM`），不探 index.html（600 s CDN 缓存骗人）。edited by Claude Fable 5.1
- **调参纪律（user 2026-09-08 拍板，全仓）**：每个有物理意义的参数按自己的量纲独立定义（米/度/CSS px/秒），不从会影响一大堆东西的共享参数推导；想改 a 就改 a 的常量，没有就先立一个。反例 = 为改 Z 轴线容差去动 VR 虚拟屏视场（A17 记录）。
- **版本 SSoT = `src/version.ts`**（`./bump.sh vX.Y.Z-YYYY-MM-DD`）；package.json 只跟着记。minor = 新纪元需人类同意（0.3.0 = user 2026-09-02 定调）。
- **自测入口**：`npm test`（node 直跑 219 绿）；`node scripts/probe-boot.mjs`（headless 开机+矩形+推拉+撤销+截图）+ `test/probes/probe-{hover,pen,touch,walk,xr-fake}.mjs`（合成事件：悬停吸附 / Pencil 落笔 / 多指 tap 撤销 / 步行相机挡墙跳盒冻结瞬移 / 假 XR 会话全链路）+ `test/probes/probe-{pp-feedback,snap-jitter}.mts`（求解器回路），都借 WeebPaint 的 playwright；**user：「你能自己探针我就不帮你测了」=探针即验收**）。
- **结构**：`src/kernel/`（几何内核，改核心必回写立宪页）→ `src/editor/`（camera 纯数学两制正交/透视 · pick/solver 对齐引擎 · tools · render3=**唯一 three 出口**，Workbench 雏形：统一灰平光+粗线 polygonOffset 治 z-fight · editor.ts=工具手势状态机〔从 lab main.ts 原样搬入，语义一字未动〕· journal=undo 日志重放 · presets=实验台预置 · obj-io）→ `src/app/`（main 接线 · gestures 指针路由 · pwa-shell · `ui/` 三件替身：popup-menu/notice/icon **签名照抄 WeebPaint src/ui/，等 `@internal/workbench-elements` 包出来换 import**——user 2026-09-06 拍板：包名定、由 CatsUp session 抽、不急）。`scripts/build.sh` 有 lint：experiments 隔离 / 对齐引擎无旧鬼 / three 只在 render3 / sprite 内联对账。
- **实验台**：菜单「实验台」= 原 lab 面板（膜事件日志 + 场景预置 + 删面钮），`?lab` 也能开；`experiments/` 仍是可丢区。
- **图标**：`python3 "../20260708 SVG Icons/extract-icons.py" assets/icons.svg <ids> --catalog --overwrite` → `python3 scripts/inline-sprites.py`（build 门）。`push-pull` 是 fable 2026-09-06 自画的**待过目**图标（库 PENDING 层），user 过目前别当定稿。
- **坐标约定（2026-09-01 立，edited by Claude Fable 5）**：**右手系，+Z 上（SU 蓝轴），+X 红，+Y 绿=北**；X×Y=+Z。渲染适配层不得引入 Y-up（three 只在 render3.ts，相机全由 camera.ts 纯数学喂）；SU 同款 RGB=XYZ 配色是家族契约。OBJ 出入口在文件边界做 Y-up 互换，仓内永远 Z-up。
- **开工前必读①：`ai-docs/20260902-kernel-constitution.md`（立宪一页——改核心必回写，铁律）**；**②`ai-docs/20260906-ledger-next-agents.md`（待办总账：一条 = 一个新 agent 的活，状态词 待做/待拍板/待看/等 user 数据/park/done；新冒出来的事只进这里，做完回写 done）**；③`ai-docs/20260906-app-shell-epoch-landing.md`（0.3 开工落地）；④`ai-docs/20260906-far-horizon-golden-format-and-ontology.md`（user 远景剧透原话：两种编辑模式 / group=同 mesh 编辑时语义、component=object / 渲染=三角汤 / glTF 黄金格式判断 / 渲染切口在引擎不抽象 three——**未来纪元开工前必读，别重新发明**）；⑤`ai-docs/20260728-phase0-kickoff-handoff.md`（里程碑、仓库纪律）。
- 几何 spec 本体 = `journals/20260627 SketchUp drill.md`（五公理 / face-finding 管线 / 膜生命周期事件表）。`journals/` 是纯人类区：AI 永不写、不删、不整理（`journals/*discussion thread.md` = user 抄的聊天记录，不当 spec 读）。
- 历史：**move 已按 sticky geometry 协议重写落地**（spec=`ai-docs/20260901-move-spec.md`）；snap 模型 SSoT=`ai-docs/20260901-snap-model.md`；pp grill 单=`ai-docs/20260901-pushpull-grill-sheet.md`；backlog 总索引=`ai-docs/20260901-backlog-index.md`。
