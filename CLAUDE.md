# CatsUp（家族总规则见上级 CLAUDE.md）

SketchUp 开源 PWA 平替。**MVP = SketchUp clone；proposal「我会加的东西」整节全是花，不可本末倒置**（`journals/20260627 CatsUp proposal.md` L80 用户原话）。

- **当前纪元：0.3 app 壳纪元（2026-09-06 开工，edited by Claude Fable 5.1）**——lab 退役，正式 app = 根 `index.html` + `src/app/`；**无地骑士：零持久化**（无 store / 无自家文件格式，user 2026-09-06「现在定文件格式还太早」），模型只活在内存，**OBJ 是唯一逃生口**（`src/editor/obj-io.ts`，Blender 往返；不是文件格式）。**iPad 一等公民**（pen=工具、手指=相机、双指轻点撤销；`src/app/gestures.ts`）。**预期快速重构**（user 原话：lab 期间忍着没提的 UX 需求现在会陆续来）。
- **上线道 = 家族「site 仓道」首案**：源仓私有（origin=OneDrive bare），构建产物推公仓 `fangzhangmnm/catsup`（user 2026-09-06 定名：「site 是错语义」；Pages 从 main 根服务，无 workflow）。`bash scripts/build.sh` → `bash scripts/push-dist.sh dev` → **https://fangzhangmnm.github.io/catsup/dev/**。今天公仓只收 dist；毕业（公开工坊道 grill）后源仓含审过的 ai-docs 替换同名公仓历史，URL 不变。**`push-dist.sh prod` = 硬规则 #5，必须人类明确指令**（脚本有 `CATSUP_PROD_OK=yes` 门）。dist/ 进 git（家族约定：各自 commit 好 dist，部署只搬不重 build）。
- **版本 SSoT = `src/version.ts`**（`./bump.sh vX.Y.Z-YYYY-MM-DD`）；package.json 只跟着记。minor = 新纪元需人类同意（0.3.0 = user 2026-09-02 定调）。
- **自测入口**：`npm test`（node 直跑 146 绿）；`node scripts/probe-boot.mjs`（headless 开机+矩形+推拉+撤销+截图，借 WeebPaint 的 playwright；**user：「你能自己探针我就不帮你测了」=探针即验收**）。
- **结构**：`src/kernel/`（几何内核，改核心必回写立宪页）→ `src/editor/`（camera 纯数学两制正交/透视 · pick/solver 对齐引擎 · tools · render3=**唯一 three 出口**，Workbench 雏形：统一灰平光+粗线 polygonOffset 治 z-fight · editor.ts=工具手势状态机〔从 lab main.ts 原样搬入，语义一字未动〕· journal=undo 日志重放 · presets=实验台预置 · obj-io）→ `src/app/`（main 接线 · gestures 指针路由 · pwa-shell · `ui/` 三件替身：popup-menu/notice/icon **签名照抄 WeebPaint src/ui/，等 `@internal/ui` 包立户后换 import**——2026-09-06 已与 WeebPaint agent 对齐，立户等 user 拍板）。`scripts/build.sh` 有 lint：experiments 隔离 / 对齐引擎无旧鬼 / three 只在 render3 / sprite 内联对账。
- **实验台**：菜单「实验台」= 原 lab 面板（膜事件日志 + 场景预置 + 删面钮），`?lab` 也能开；`experiments/` 仍是可丢区。
- **图标**：`python3 "../20260708 SVG Icons/extract-icons.py" assets/icons.svg <ids> --catalog --overwrite` → `python3 scripts/inline-sprites.py`（build 门）。`push-pull` 是 fable 2026-09-06 自画的**待过目**图标（库 PENDING 层），user 过目前别当定稿。
- **坐标约定（2026-09-01 立，edited by Claude Fable 5）**：**右手系，+Z 上（SU 蓝轴），+X 红，+Y 绿=北**；X×Y=+Z。渲染适配层不得引入 Y-up（three 只在 render3.ts，相机全由 camera.ts 纯数学喂）；SU 同款 RGB=XYZ 配色是家族契约。OBJ 出入口在文件边界做 Y-up 互换，仓内永远 Z-up。
- **开工前必读①：`ai-docs/20260902-kernel-constitution.md`（立宪一页——改核心必回写，铁律）**；②`ai-docs/20260906-app-shell-epoch-landing.md`（0.3 开工落地 + 遗留清单）；③`ai-docs/20260906-far-horizon-golden-format-and-ontology.md`（user 远景剧透原话：两种编辑模式 / group=同 mesh 编辑时语义、component=object / 渲染=三角汤 / glTF 黄金格式判断 / 渲染切口在引擎不抽象 three——**未来纪元开工前必读，别重新发明**）；④`ai-docs/20260728-phase0-kickoff-handoff.md`（里程碑、仓库纪律）。
- 几何 spec 本体 = `journals/20260627 SketchUp drill.md`（五公理 / face-finding 管线 / 膜生命周期事件表）。`journals/` 是纯人类区：AI 永不写、不删、不整理（`journals/*discussion thread.md` = user 抄的聊天记录，不当 spec 读）。
- 历史：**move 已按 sticky geometry 协议重写落地**（spec=`ai-docs/20260901-move-spec.md`）；snap 模型 SSoT=`ai-docs/20260901-snap-model.md`；pp grill 单=`ai-docs/20260901-pushpull-grill-sheet.md`；backlog 总索引=`ai-docs/20260901-backlog-index.md`。
