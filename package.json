{
  "name": "catsup",
  "version": "0.2.6",
  "private": true,
  "type": "module",
  "comment": "无壳纯 TS 几何内核（Phase 0）。运行时零依赖；node 24 strip-types 直跑 test/；devDependencies 仅供 tsc --noEmit 类型门。工具链抄 WebPaint 现役（runner.mjs / build.sh / tsconfig）。",
  "scripts": {
    "test": "node test/run.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "bash scripts/build.sh"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
